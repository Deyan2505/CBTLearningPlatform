# 22 — User Flows

Формат за всеки поток: начална точка → стъпки → крайна цел → грешки/празни състояния → accessibility изисквания → acceptance criteria.

## 1. Първо посещение

- **Начална точка:** директен линк или търсачка → начална страница.
- **Стъпки:** зарежда се начална страница → вижда заглавие/ценностно предложение → вижда кратко "какво НЕ е това" → избира "Начни" или "Какво е КПТ".
- **Крайна цел:** посетителят разбира предложението за <60 сек и решава да продължи.
- **Грешки/празни състояния:** N/A (статична страница).
- **Accessibility:** heading йерархия (h1 еднократно), skip-to-content линк, всички CTA бутони достижими с Tab.
- **Acceptance criteria:** Lighthouse/axe без критични грешки; страницата се зарежда <2 сек на средна мрежа.

## 2. Запознаване с CBT

- **Начална точка:** `/kpt` (от навигация или от начална страница).
- **Стъпки:** чете кратко обяснение → вижда disclaimer → вижда CTA към програмата.
- **Крайна цел:** основно разбиране + преход към програмата.
- **Грешки/празни състояния:** N/A.
- **Accessibility:** същите принципи като поток 1.
- **Acceptance criteria:** Disclaimer видим без scroll на десктоп; достъпен веднага на мобилен.

## 3. Избор на модул

- **Начална точка:** `/programa` (каталог).
- **Стъпки:** вижда списък модули с кратко описание и индикатор за прогрес (0/N уроци) → избира модул.
- **Крайна цел:** влизане в модул.
- **Грешки/празни състояния:** ако local storage е празен/недостъпен (напр. private browsing) — индикаторът показва "0 завършени", не грешка.
- **Accessibility:** карти на модули достижими с клавиатура, ясен фокус ринг, `aria-label` с прогреса.
- **Acceptance criteria:** каталогът рендира коректно и при 1, и при бъдещи N модула.

## 4. Започване на урок

- **Начална точка:** страница на модул.
- **Стъпки:** избира урок от списъка → чете съдържание → стига до "проверка на разбирането".
- **Крайна цел:** усвоено съдържание, готовност за проверка.
- **Грешки/празни състояния:** N/A.
- **Accessibility:** четимо форматиране (макс. ширина на текстов блок ~65-75 знака), заглавия в логическа йерархия.
- **Acceptance criteria:** урокът е напълно четим и навигируем само с клавиатура и само с screen reader.

## 5. Завършване на урок

- **Начална точка:** край на съдържанието на урока.
- **Стъпки:** отговаря/размишлява по проверката на разбирането → маркира урока като завършен (автоматично или чрез бутон) → local storage се обновява → вижда следваща препоръчана стъпка.
- **Крайна цел:** урокът е маркиран завършен, прогресът се пази.
- **Грешки/празни състояния:** ако local storage запис се провали (напр. quota) — тих fallback, урокът остава четим, но без запис на прогрес; не блокираща грешка за потребителя.
- **Accessibility:** потвърждението за завършване е обявено чрез `aria-live` регион за screen reader потребители.
- **Acceptance criteria:** прогресът е видим при следващо посещение на същия браузър.

## 6. Проверка на разбирането

- **Начална точка:** край на урок.
- **Стъпки:** чете рефлективен въпрос → пише/размишлява (без изпращане към сървър) → продължава.
- **Крайна цел:** затвърждаване, не оценка.
- **Грешки/празни състояния:** N/A — няма "грешен отговор" в рефлективен формат (REQ-FUNC-003).
- **Accessibility:** textarea (ако има поле за писане) с ясен label.
- **Acceptance criteria:** потребителят никога не вижда "правилно/грешно" съобщение в MVP формата.

## 7. Изпълнение на упражнение (thought record)

- **Начална точка:** Модул 9, вграденото упражнение.
- **Стъпки:** попълва ситуация → автоматична мисъл → емоция/интензитет → насочващи въпроси → балансирана мисъл → запазва локално.
- **Крайна цел:** попълнен, личен пример, видим резултат преди/след.
- **Грешки/празни състояния:** празни задължителни полета — inline валидационно съобщение, не блокиращ alert; local storage недостъпен — предупреждение, но упражнението остава използваемо в рамките на сесията.
- **Accessibility:** всяко поле с програмно свързан `<label>` (REQ-A11Y-003 — директен коректив на слабостта, открита в `16_PROTOTYPE_TECHNICAL_AUDIT.md` т.8); грешки съобщени чрез `aria-describedby`.
- **Acceptance criteria:** упражнението е напълно завършимо само с клавиатура; данните не напускат браузъра (без мрежова заявка към трета страна — коректив на GAP-007/REQ-SEC-004).

## 8. Преглед на напредъка

- **Начална точка:** индикатор в каталога с модули или отделна секция.
- **Стъпки:** вижда общ % завършено → вижда кои уроци/модули са завършени.
- **Крайна цел:** мотивация за продължаване, прозрачност.
- **Грешки/празни състояния:** 0% е валидно, нормално състояние (не грешка) при първо посещение.
- **Accessibility:** прогрес индикатор с текстова алтернатива (не само визуален progress bar).
- **Acceptance criteria:** числото съвпада точно с реално завършените уроци.

## 9. Връщане към незавършен урок

- **Начална точка:** ново посещение, същия браузър.
- **Стъпки:** отваря каталога → вижда кой урок е "следващ препоръчан" → продължава.
- **Крайна цел:** безпроблемно продължение без загуба на прогрес.
- **Грешки/празни състояния:** ако local storage е бил изчистен (потребителят е изчистил браузъра) — прогресът legitimately изчезва; UI показва "начало", не грешка.
- **Accessibility:** същите принципи.
- **Acceptance criteria:** прогресът преживява презареждане на страницата и затваряне/отваряне на браузъра (не и изчистване на данни на браузъра — очаквано ограничение на local storage подхода, комуникирано на потребителя).

## 10. Намиране на източник

- **Начална точка:** всяко клинично твърдение в урок.
- **Стъпки:** вижда inline citation маркер → кликва/докосва → вижда пълната референция (SRC-ID + локатор).
- **Крайна цел:** проверимост на твърдението.
- **Грешки/празни състояния:** N/A — всяко клинично твърдение задължително носи citation (REQ-CONT-004); липсваща citation е content bug, не runtime грешка.
- **Accessibility:** citation маркерите са достижими и четими от screen reader (не само visual superscript).
- **Acceptance criteria:** 100% от клиничните твърдения в публикувано съдържание имат видима citation.

## 11. Намиране на информация за професионална помощ

- **Начална точка:** навсякъде — footer линк, контекстуален линк в уроци, Модул 14.
- **Стъпки:** кликва линк → стига до Модул 14 директно, без допълнителна навигация.
- **Крайна цел:** незабавен, недвусмислен достъп до тази информация от всяка точка в приложението.
- **Грешки/празни състояния:** N/A — този линк не трябва никога да липсва.
- **Accessibility:** линкът е в постоянна, предвидима позиция (footer) на всяка страница.
- **Acceptance criteria:** линкът "Кога е нужна професионална помощ" присъства в 100% от страниците с психологическо съдържание; проверявано в QA чеклист (`06_QA_STRATEGY.md`).

## 12. Използване от мобилен телефон

- **Начална точка:** всеки поток по-горе, на мобилно устройство.
- **Стъпки:** идентични на десктоп потоците, с responsive преформатиране.
- **Крайна цел:** пълна функционална равностойност с десктоп.
- **Грешки/празни състояния:** N/A.
- **Accessibility:** тъч цели минимум 44×44px; без хоризонтално превъртане на никоя страница.
- **Acceptance criteria:** ръчно тествано на 375px viewport (REQ-UX-005).

## 13. Разглеждане на 15-седмичния курс (ново, ADR-010, Сесия 18)

- **Начална точка:** sidebar навигация → "15-седмичен курс" (`/kurs`).
- **Стъпки:** вижда intro + разграничение "пълна програма срещу налично сега" (progressive disclosure) → вижда 4-те curriculum блока → разглежда списъка от 15 седмици с честен статус етикет за всяка → кликва една от двете налични седмици (Седмица 1 или Седмица 8).
- **Крайна цел:** ясно разбиране кое от 15-седмичната академична програма реално съществува като образователно съдържание точно сега, без dead links или подвеждащи очаквания.
- **Грешки/празни състояния:** N/A — статичен списък, без мрежови заявки.
- **Accessibility:** списъкът е семантичен (`<ol>`/`aria-label`), статус показан и текстово (не само чрез цвят на badge-а), progressive disclosure е native `<details>`.
- **Acceptance criteria:** 0 от 15-те седмични записа сочат към несъществуваща страница; статус етикетите съвпадат точно с реалната наличност на route.
- **[Сесия 25]** Две седмици вече налични (Седмица 1 добавена, CONTENT-DRIVEN TEMPLATE VALIDATION Slice 1) — start panel и timeline сочат и към двете, останалите 13 остават непроменени в статус.

## 14a. Разглеждане на представителна седмица — "Theory and History" архетип (Седмица 1, Сесия 25)

- **Начална точка:** `/kurs`, клик върху "Седмица 1" (или section-nav anchor директно).
- **Стъпки:** чете "Накратко" → разглежда `HistoricalTimeline` (6 milestones, semantic `<ol>`, без CSS overflow workaround) → взаимодейства с единствения interactive island `ResearchTurnStepper` (адаптиран от `CbtModelDiagram` — 4 стъпки хипотеза→проверка→резултат→преформулиране, без storage/scoring) → разглежда `comparison-matrix--dual` таблицата "какво се променя във фокуса" → чете preview на автоматичните мисли (CTA към Седмица 8, не дублира симулатора) → по избор разгръща "Защо протоколите имат значение" → разглежда 1979 milestone card → работи с 3-въпросна knowledge check (native `<details>/<summary>` reveal, не нов quiz engine) → чете обобщение → по избор разгръща академичния контекст (разграничение curriculum source vs. academic source) → вижда educational disclaimer с custom текст (нов `DisclaimerCallout.Text` override параметър, backward-compatible).
- **Крайна цел:** втори, съдържателно различен representative-week архетип (Theory and History, не Simulator Workspace) валидира, че Weekly Course Hub архитектурата (ADR-010) реално мащабира отвъд Седмица 8, без нов global redesign.
- **Грешки/празни състояния:** N/A.
- **Accessibility:** идентични принципи на поток 14; `HistoricalTimeline`/`comparison-matrix--dual` са нула нови CSS класове (wholesale reuse на вече проверени `.week-timeline`/`.comparison-matrix--dual` стилове); `ResearchTurnStepper` има текстов fallback (`<details>`) и `aria-current="step"`.
- **Acceptance criteria:** exactly 1 interactive island на страницата; disclaimer/source references никога скрити зад disclosure; всички cross-links (`/kpt`, Модул 1, Седмица 8, `/kurs`) сочат към реални routes; без ECTS/институционален бранд/диагностично съдържание; единствената точна година, показана на страницата (1979), е потвърдена срещу SRC-041 (GAP-012 в `15_GAPS_AND_CONFLICTS.md` вече Closed); по-ранните milestones нарочно остават без точни години, макар 1964 да е също потвърдена — съзнателно решение да не се добавя точност, надхвърляща дадения инструктаж.
- **[Сесия 26, owner review correction]** Sidebar вече показва "weak context" маркер на "15-седмичен курс" при посещение на всяка `/kurs/*` подстраница (преди тази сесия — 0 маркирани sidebar items на подстраници, потребителят губеше визуален контекст къде се намира спрямо цялото приложение). Същият принцип приложен и на `/programa/*` (вкл. lesson routes) за консистентност. `ResearchTurnStepper` вече използва локален responsive CSS Grid (4 равноправни стъпки на широк desktop, без dangling arrow, без absolute positioning) вместо споделения `.cbt-diagram` flex-wrap, който се чупеше неравномерно с по-дългите stepper labels. `HistoricalTimeline` вече поддържа `Compact` density вариант, приложен на Седмица 1 (Course Hub-овата собствена timeline остава непроменена). Публичният "Академичен контекст" вече е чисто learner-facing — без файлови имена/вътрешен QA жаргон.

## 14b. Разглеждане на представителна седмица — "Concept and Diagram" архетип (Седмица 3, Сесия 28)

- **Начална точка:** `/kurs`, клик върху "Седмица 3" (или section-nav anchor директно).
- **Стъпки:** разглежда статичната йерархия от три нива (`.learning-path-diagram` reuse) → взаимодейства с `CognitiveHierarchyExplorer` (избира едно от трите нива, вижда описание/общост/типична форма/връзка със съседните нива/пример) → разглежда situation-to-meaning `.concept-map__flow` диаграма → чете за когнитивната триада (статичен 3-box grid + по избор разгъва историческия контекст) → взаимодейства със `SchemaFilterDemonstration` (превключва "покажи всички данни"/"покажи как очакването насочва вниманието"/reset — фиксиран, неутрален сценарий) → сравнява автоматична срещу рефлексивна обработка (два `.card` списъка + процес диаграма) → разглежда интегрираната concept map (централният visual anchor, 5 нива + feedback бележка) → работи с "чести обърквания" categorization check (native `<details>` reveal, 3 фиксирани фрази) → работи с 4-въпросна knowledge check → чете обобщение → по избор разгръща академичния контекст → вижда educational disclaimer.
- **Крайна цел:** трети, съдържателно различен representative-week архетип (Concept and Diagram, нито Theory and History, нито Simulator Workspace) валидира, че Weekly Course Hub архитектурата (ADR-010) реално мащабира и към йерархично/диаграмно съдържание.
- **Грешки/празни състояния:** N/A — и двата interactive компонента използват затворени избори (бутони), без свободен текст.
- **Accessibility:** идентични принципи на поток 14/14a; `CognitiveHierarchyExplorer` reuse-ва `.cbt-diagram` (текстов fallback, `aria-current="step"`); `SchemaFilterDemonstration` използва `aria-live` за променящия се списък и никога не разчита само на цвят — всяка визуална промяна (emphasis/dimming) е придружена от текстов label.
- **Acceptance criteria:** exactly 2 interactive island-а на страницата; disclaimer/source references никога скрити зад disclosure; всички cross-links (Модул 2 + 2 урока, Седмица 1, Седмица 8, `/kurs`) сочат към реални routes; без линк към несъществуваща Седмица 4; без ECTS/диагностични скали (BDI/BAI/BHS)/самодиагностика/терапевтични инструкции; основните вярвания не са представени като задължително отрицателни; схемата изрично обозначена като метафора.

## 14c. Разглеждане на представителна седмица — "Guided Practice" архетип (Седмица 10, Сесия 32)

- **Начална точка:** `/kurs`, клик върху "Седмица 10" (или section-nav anchor директно).
- **Стъпки:** чете процес диаграмата на сократическото изследване (`.concept-map__flow`) + двуколонно сравнение "изследване срещу убеждаване" → разглежда четирите семейства въпроси (`.concept-map__side-notes`, 4 карти) → взаимодейства със `SocraticDialogueExplorer` (5 стъпки върху фиксиран неутрален сценарий — изясняване/доказателства/алтернативи/последствия/балансирано заключение) → работи с "въпрос или прикрит съвет" categorization check (native `<details>` reveal, 4 фиксирани примера) → разглежда факт/предположение/заключение сравнението (`.category-compare` reuse) → по избор разгръща декатастрофизиране обяснението → сравнява прекалено отрицателно/принудително положително/балансирано → разглежда practice flow диаграмата → работи с 4-въпросна knowledge check → чете обобщение → по избор разгръща академичния контекст → вижда educational disclaimer.
- **Крайна цел:** четвърти, съдържателно различен representative-week архетип (Guided Practice, нито Theory and History, нито Concept and Diagram, нито Simulator Workspace) валидира, че Weekly Course Hub архитектурата (ADR-010) реално мащабира и към насочени, non-scored въпросни упражнения.
- **Грешки/празни състояния:** N/A — единственият interactive компонент използва затворени избори (бутони), без свободен текст.
- **Accessibility:** идентични принципи на поток 14/14a/14b; `SocraticDialogueExplorer` reuse-ва `.cbt-diagram` (текстов fallback, `aria-current="step"`).
- **Acceptance criteria:** exactly 1 interactive island на страницата; disclaimer/source references никога скрити зад disclosure; всички cross-links (Седмица 3, Седмица 8, урока за автоматичните мисли, Модул 2, `/kurs`) сочат към реални routes; без линк към несъществуваща Седмица 11; без ECTS/диагностични скали/клинично-обучителен език; балансираният отговор не е представен като принудителен позитивизъм; декатастрофизирането не омаловажава реален проблем.

## 14. Разглеждане на представителна седмица (Седмица 8, ADR-010, Сесия 18; преработено съдържателно Сесия 19)

- **Начална точка:** `/kurs`, клик върху "Седмица 8" (или section-nav anchor директно).
- **Стъпки:** чете "Накратко" (кратко определение + key-idea chips + мини схема) → по избор скача чрез section-nav → разглежда статичната карта на темата (concept map + мини сравнения) → взаимодейства с `CbtChainSimulator` (избира ситуация/мисъл/емоция/интензитет/поведение, вижда live обновявана верижна диаграма, може да нулира) → разглежда branch diagram-а (двата пътя на `InterpretationExample`, по избор подчертава един) → работи с `CategorizationCheck` workspace (легенда + reveal примери + reset) → по избор разгръща пълното обяснение → размишлява по проверката на разбирането → чете обобщение → по избор разгръща академичния контекст → вижда disclaimer.
- **Крайна цел:** усещане за интерактивен, многослоен, визуално разграничен учебен работен пространство (не поредица статични текстови страници) — без да се въвежда ново клинично съдържание извън вече одобреното в Модул 2.
- **Грешки/празни състояния:** N/A — всички избори в симулатора са затворени (select/slider), няма невалидно състояние.
- **Accessibility:** идентични принципи на поток 4/13; всички disclosure нива са keyboard-операбилни native `<details>`; section anchors имат `tabindex="-1"` за focus след навигация; симулаторните полета имат `<label for>`; живият изход използва `aria-live`; branch diagram-ът не разчита само на цвят за emphasis (border-width + font-weight).
- **Acceptance criteria:** disclaimer и source references никога не са скрити зад disclosure; и трите линка към пълните уроци водят към реални, съществуващи страници; симулаторът не изисква личен свободен текст и не съхранява нищо.
- **[Сесия 20]** Секциите вече се разполагат в 5 двуколонни реда на desktop (≥900px) — Накратко+Карта на темата, Симулатор controls-срещу-output, Сравнение+Мисъл или емоция, Пълно обяснение+Академичен контекст, Проверка+Обобщение; под 900px — единична колона в същия DOM/reading ред (без CSS `order`). Всяка секция носи визуален color-role marker (border+badge), без да разчита само на цвят (текстовият badge label е винаги четим).
- **[Сесия 21]** Двуколонният pattern вече важи за целия сайт, не само Седмица 8/Kurs — потокът "първо посещение" (поток 1) вече минава през реално двуколонен Home (hero + 3 двуколонни реда) вместо едноколонна текстова страница; "избор на модул" (поток 3) вече минава през wide-narrow module overview с visual sequence sidebar; всеки "започване на урок" (поток 4) вече включва поне един конкретен visual anchor (сравнителна таблица/процес диаграма/emotion-body diagram), не само параграфи. Breakpoint-ът за двуколонния layout вече е container query (реалната ширина на workspace зоната след sidebar-а), не viewport — по-точен на широки desktop екрани.
- **[Сесия 22]** "Използване само с клавиатура/навигация" (поток 15) вече минава през коригиран sidebar — точно 1 active елемент на всеки route, без объркващо двойно "active" усещане. "Започване на урок" (поток 4) вече включва изрична progressive-disclosure стъпка: кратка дефиниция+3 извода видими веднага, "Покажи пълното обяснение" за разширения текст — учебните цели/дефиницията/visual anchor/disclaimer остават винаги видими, никога зад disclosure.
- **[Сесия 23]** "Избор на модул" (поток 3) вече минава през реален `.module-path` — номериран, кликаем node за наличния урок, ясно разграничен (не-кликаем, dashed marker) status node за кратка резултатна информация, и отделен "напред" node към следващия модул; вече не е плосък списък от заглавия. Фокусът при навигация (Blazor `<FocusOnNavigate>`) вече не показва бутон-стил рамка около обикновен `<h1>` при нормално зареждане — само при реален клавиатурен Tab до heading.

## 15. Използване само с клавиатура

- **Начална точка:** всеки поток по-горе.
- **Стъпки:** пълна навигация чрез Tab/Shift+Tab/Enter/Space/Arrow keys, без мишка.
- **Крайна цел:** пълна функционална равностойност.
- **Грешки/празни състояния:** N/A.
- **Accessibility:** видим focus indicator на всеки интерактивен елемент (REQ-UX-003, директен коректив на слабост в `16_PROTOTYPE_TECHNICAL_AUDIT.md` т.7); логичен tab order; без keyboard traps.
- **Acceptance criteria:** ръчен QA преминава всичките 14 потока по-горе само с клавиатура, без блокиращи точки.
